# BakimX v0.5.0 — Manual QA Checklist

## Communication Provider Setup

- [ ] App starts with `SMS_PROVIDER=mock` (default) without errors
- [ ] App starts with `WHATSAPP_PROVIDER=mock` (default) without errors
- [ ] App starts with `EMAIL_PROVIDER=mock` (default) without errors
- [ ] Setting `SMS_PROVIDER=netgsm` without `NETGSM_USERCODE`/`NETGSM_PASSWORD` throws clear error
- [ ] Setting `EMAIL_PROVIDER=resend` without `RESEND_API_KEY` throws clear error
- [ ] Setting `WHATSAPP_PROVIDER=business` without `WHATSAPP_PHONE_NUMBER_ID`/`WHATSAPP_ACCESS_TOKEN` throws clear error

## Notification Center (/app/settings/notifications)

- [ ] Page loads showing all 8 template types with channel badges
- [ ] Active providers (SMS, WhatsApp, Email) shown with current provider names
- [ ] Clicking a template expands to show per-channel content
- [ ] Default templates are displayed correctly for each channel
- [ ] Editing a template shows textarea with current content
- [ ] Saving a template persists the custom content (page refresh shows it)
- [ ] "Reset to Default" clears custom template and shows default again
- [ ] Template sanitization strips `<script>` tags and event handlers
- [ ] Template variable hints shown below editor
- [ ] Breadcrumb navigation works (Ana Panel > Ayarlar > Bildirim Ayarları)

## Communication Log (/app/communications)

- [ ] Page loads showing communication log table
- [ ] Stats cards show sent, failed, pending counts correctly
- [ ] Type filter (SMS, WhatsApp, Email) works
- [ ] Status filter (Gönderildi, Başarısız, Bekliyor) works
- [ ] Empty state shown when no logs exist
- [ ] Each log row shows type, recipient, template, status, provider, date

## Appointment Notifications

- [ ] Creating a new appointment triggers notification (check console in mock mode)
- [ ] Notification respects customer SMS/WhatsApp/Email consent preferences
- [ ] Notification logged with correct template key "appointment_created"

## Intake Approval Notifications

- [ ] Requesting approval on an intake form triggers SMS + WhatsApp notification
- [ ] Approval link is included in notification content
- [ ] Notification respects customer consent preferences

## Quote Notifications

- [ ] Changing quote status to "sent" triggers notification
- [ ] Quote number and total amount included in notification
- [ ] Notification respects customer consent preferences

## Work Order Completion

- [ ] Setting order status to "ready_for_delivery" triggers completion notification
- [ ] Work order number included in notification
- [ ] Setting order status to "delivered" with unpaid payment triggers payment reminder
- [ ] Both notifications respect customer consent preferences

## Maintenance Reminder

- [ ] Creating a reminder with preferred channel triggers notification
- [ ] Reminders with channel "none" do not trigger notifications
- [ ] Maintenance type and due date included in notification

## Customer Communication Preferences

- [ ] Customer create form includes SMS/WhatsApp/Email consent checkboxes
- [ ] Customer update form includes consent checkboxes
- [ ] Consent changes persist after save
- [ ] When consent is unchecked, notifications are not sent for that channel
- [ ] KVKK approval timestamp is set when any consent is granted

## Reminder Scheduling

- [ ] Cron endpoint `/api/cron/reminders` responds to GET request (in dev, without CRON_SECRET)
- [ ] Cron endpoint requires `Authorization: Bearer <CRON_SECRET>` in production
- [ ] Appointment reminders: appointments within 24h with `reminderStatus=pending` get notifications
- [ ] Appointment reminders: duplicate send prevention via `lastRemindedAt` (24h reminder skips if sent <12h ago)
- [ ] Appointment reminders: 1h reminder skips if sent <30min ago
- [ ] Maintenance reminders: upcoming/due_soon/overdue with `preferredChannel != none` get notifications
- [ ] Maintenance reminders: `lastRemindedAt` prevents duplicate sends within 24h
- [ ] After successful reminder, `reminderStatus` updated to `sent` and `lastRemindedAt` set
- [ ] After failed reminder, `reminderStatus` updated to `failed` and `lastRemindedAt` set

## Security

- [ ] Communication logs are scoped to workshop (no cross-tenant data leakage)
- [ ] API routes require authentication
- [ ] Templates do not render raw HTML from user input (sanitization works)
- [ ] Rate limiting prevents sending more than 30 messages per customer per channel per minute
- [ ] No API keys or secrets are exposed in template content or log output

## Navigation

- [ ] Sidebar shows "İletişim Kayıtları" under "İletişim" group
- [ ] Sidebar shows "Bildirim Ayarları" under "Ayarlar" group
- [ ] Both links navigate correctly
- [ ] Mobile bottom nav still works correctly

## Database

- [ ] `prisma db push` applies new enums (CommunicationType, CommunicationStatus)
- [ ] `prisma db push` creates CommunicationTemplate table
- [ ] `prisma db push` creates CommunicationLog table
- [ ] Unique constraint on (workshopId, templateKey, channel) works
- [ ] All indexes created correctly

## Regression

- [ ] All existing features work as before (appointments, intakes, orders, quotes, reminders)
- [ ] Customer create/edit still works with consent fields
- [ ] App build succeeds (`bun run build`)
- [ ] Lint passes (`bun run lint`)
- [ ] Typecheck passes (`bun run typecheck`)