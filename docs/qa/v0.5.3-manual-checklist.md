# BakimX v0.5.3 — Manual QA Checklist

## Settings Page (/app/settings)

- [ ] Page loads without errors
- [ ] Tab navigation works: clicking each tab switches content
- [ ] URL updates with `?tab=` query param on tab change
- [ ] Active tab is visually highlighted (blue border, blue text)
- [ ] All 7 tabs are visible: İş Yeri Profili, Marka Ayarları, İletişim Ayarları, Çalışma Saatleri, Randevu Kuralları, PDF Şablonları, Güvenlik

## Business Profile (İş Yeri Profili)

- [ ] Form loads with existing workshop data pre-filled
- [ ] Required fields show validation errors when empty (name, phone, city, address)
- [ ] Saving valid data shows green success message "Bilgiler güncellendi"
- [ ] Invalid email format shows validation error
- [ ] Invalid URL for website shows validation error
- [ ] Invalid URL for logo shows validation error
- [ ] Tax fields (taxOffice, taxNumber, invoiceTitle) save correctly
- [ ] Empty optional fields save as null (not empty string)
- [ ] Audit log entry created for business profile update

## Branding (Marka Ayarları)

- [ ] Form loads with existing settings or empty defaults
- [ ] PDF Logo URL field accepts valid URLs
- [ ] Portal Logo URL field accepts valid URLs
- [ ] Passport Logo URL field accepts valid URLs
- [ ] Color picker syncs with text input for themeColor
- [ ] Color picker syncs with text input for accentColor
- [ ] Hex color validation rejects invalid formats
- [ ] Saving shows green success message "Marka ayarları güncellendi"
- [ ] Empty color fields save as null

## Communication Settings (İletişim Ayarları)

- [ ] Three cards render: SMS, WhatsApp, Email
- [ ] SMS provider dropdown: Mock, Netgsm, İletimerkezi, Özel
- [ ] WhatsApp provider dropdown: Mock, WhatsApp Business API, Özel
- [ ] Email provider dropdown: Mock, Resend, SendGrid, Özel
- [ ] Selecting "Mock" disables related fields and shows info banner
- [ ] API key fields use type="password" for masking
- [ ] API keys are optional — existing keys preserved if new ones not entered
- [ ] Mock provider info banners display correctly
- [ ] Saving shows green success message "İletişim ayarları güncellendi"
- [ ] Rate limiting: rapid requests return 429 status

## Working Hours (Çalışma Saatleri)

- [ ] Weekday working days toggles work (click to select/deselect)
- [ ] Weekend working days toggles work independently
- [ ] Selected days show as blue (weekday) or green (weekend)
- [ ] Time inputs show existing values
- [ ] Holiday toggle checkbox works
- [ ] When holiday enabled, holiday dates field appears
- [ ] Holiday dates placeholder shows format (YYYY-MM-DD)
- [ ] Time format validation (HH:mm) works
- [ ] Saving shows green success message "Çalışma saatleri güncellendi"
- [ ] Rate limiting: rapid requests return 429 status

## Appointment Rules (Randevu Kuralları)

- [ ] Default appointment duration input shows current value
- [ ] Buffer duration input shows current value
- [ ] Reminder timings list shows existing values
- [ ] "Add reminder" button adds new empty input
- [ ] "Remove" button removes a reminder (min 1 remains)
- [ ] Duration validation: 5-480 minutes
- [ ] Buffer validation: 0-120 minutes
- [ ] Saving shows green success message "Randevu kuralları güncellendi"
- [ ] Rate limiting: rapid requests return 429 status

## PDF Templates (PDF Şablonları)

- [ ] Three template cards render: İş Emri, Servis Pasaportu, Tahsilat Makbuzu
- [ ] Textareas show existing template content or are empty
- [ ] "Varsayılana Sıfırla" button populates default template text
- [ ] Available variables section shows correct variables for each template
- [ ] Saving shows green success message "PDF şablonları güncellendi"
- [ ] Rate limiting: rapid requests return 429 status

## Security Tab (Güvenlik)

- [ ] Workshop ID and name display correctly
- [ ] User account info shows: name, email, user ID, workshop name
- [ ] Data isolation active confirmation shows green checkmark
- [ ] Security features list shows 5 items with checkmarks

## Public Portal Branding

- [ ] `/p/[token]` page uses `publicPortalLogoUrl` when set
- [ ] `/p/[token]` page falls back to workshop `logoUrl` when settings logo not set
- [ ] `/s/[token]` page uses `publicPortalLogoUrl` when set
- [ ] `/s/[token]` page falls back to workshop `logoUrl` when settings logo not set
- [ ] Portal theme colors render when configured

## PDF Branding

- [ ] `/p/[token]/pdf` route uses `pdfLogoUrl` from settings
- [ ] `/s/[token]/pdf` route uses `passportLogoUrl` from settings
- [ ] Falls back to workshop `logoUrl` when settings logo not set

## Rate Limiting

- [ ] Settings API endpoints return 429 after 30 rapid requests per minute
- [ ] 429 response includes `Retry-After` header
- [ ] Rate limit is per-endpoint (not global)
- [ ] Login endpoint rate limiting works independently

## Validation

- [ ] `businessProfileSchema` validates required fields and URL formats
- [ ] `brandingSchema` validates hex color codes (#RRGGBB)
- [ ] `communicationSettingsSchema` validates provider enums
- [ ] `workingHoursSchema` validates HH:mm time format
- [ ] `appointmentRulesSchema` validates duration ranges
- [ ] `pdfTemplateSchema` allows empty templates

## Security

- [ ] All settings pages require authentication
- [ ] Settings data is scoped to the authenticated user's workshop
- [ ] No cross-tenant data leakage
- [ ] API keys are not exposed in GET responses (masked in UI)
- [ ] Audit log entries created for all settings changes
- [ ] Rate limiting prevents brute-force on login

## Database

- [ ] `bunx prisma validate` passes
- [ ] `bunx prisma generate` succeeds
- [ ] `bunx prisma db push` creates WorkshopSettings table
- [ ] Auto-creation of WorkshopSettings row works on first GET

## No Docker

- [ ] No Dockerfile present in project
- [ ] No docker-compose.yml present in project

## No Secrets

- [ ] No API keys, passwords, or tokens committed to repository
- [ ] `.env.local` is in `.gitignore`
- [ ] API key inputs use type="password"

## Regression

- [ ] Workshop onboarding page (/app/workshop) loads and saves correctly
- [ ] Workshop update API still works with existing fields
- [ ] Login flow works normally
- [ ] Public share pages load correctly
- [ ] PDF generation works for intake and service pages
- [ ] All existing features work as before
- [ ] `bun run lint` passes
- [ ] `bun run typecheck` passes
- [ ] `bun run build` succeeds