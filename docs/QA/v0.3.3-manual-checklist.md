# BakimX v0.3.3 — Manuel QA Checklist

**Tarih:** 2026-06-12
**Versiyon:** v0.3.3
**Odak:** Smart Capture Hardening & Real OCR Provider

---

## 1. OCR Provider Configuration

- [ ] `OCR_PROVIDER=mock` (veya boş) → Demo verisi döner, API anahtarı gerekmez
- [ ] `OCR_PROVIDER=mock` → Sağlayıcı etiketi "Demo (Mock)" gösterilir
- [ ] `OCR_PROVIDER=deepseek` + `DEEPSEEK_API_KEY` + `OCR_MODEL=deepseek-chat` → DeepSeek OCR çalışır
- [ ] `OCR_PROVIDER=deepseek` + `DEEPSEEK_API_KEY` + `DEEPSEEK_OCR_MODEL=deepseek-chat` → Geriye dönük uyumlu, çalışır
- [ ] `OCR_PROVIDER=deepseek` + `DEEPSEEK_API_KEY` eksik → Türkçe hata mesajı gösterilir
- [ ] `OCR_PROVIDER=deepseek` + `DEEPSEEK_API_KEY` var ama model yok → Türkçe hata mesajı gösterilir
- [ ] `OCR_PROVIDER=openai` + `OPENAI_API_KEY` + `OCR_MODEL=gpt-4o` → OpenAI OCR çalışır
- [ ] `OCR_PROVIDER=openai` + `OPENAI_API_KEY` + `OPENAI_OCR_MODEL=gpt-4o-mini` → Geriye dönük uyumlu, çalışır
- [ ] `OCR_PROVIDER=openai` + `OPENAI_API_KEY` eksik → Türkçe hata mesajı
- [ ] `OCR_PROVIDER=tesseract` → Yerel Tesseract OCR çalışır, API anahtarı gerekmez
- [ ] `OCR_PROVIDER=tesseract` → Sağlayıcı etiketi "Tesseract (Yerel)" gösterilir
- [ ] `OCR_PROVIDER=invalid` → Türkçe hata mesajı: "Bilinmeyen OCR sağlayıcısı"
- [ ] `OCR_MODEL` ayarlandığında sağlayıcıya özel model override eder

## 2. Image Upload Hardening

- [ ] JPEG dosya yükleme → başarılı
- [ ] PNG dosya yükleme → başarılı
- [ ] WebP dosya yükleme → başarılı
- [ ] HEIC dosya yükleme → JPEG'e dönüştürülür ve başarılı
- [ ] 8 MB'den büyük dosya → "Görsel 8 MB'dan küçük olmalıdır" hatası
- [ ] Geçersiz MIME tipi (ör: PDF) → "Desteklenmeyen görsel biçimi" hatası
- [ ] HEIC dönüştürme sonrası 8 MB sınırını aşan dosya → "Dönüştürülen görsel sınırı aşıyor" hatası
- [ ] multipart/form-data ile dosya yükleme → başarılı
- [ ] JSON body ile data URL yükleme → başarılı (geriye dönük uyumlu)
- [ ] 12 MB'den büyük istek gövdesi → 413 hatası

## 3. Smart Capture UX

- [ ] `/app/smart-capture/registration` sayfası açılır
- [ ] Fotoğraf yükleme → OCR çalışır
- [ ] Her alan yanında güven yüzdesi gösterilir (ör: %92, %78)
- [ ] Düşük güven oranlı alanlar (%70 altı) sarı vurgu ile işaretlenir
- [ ] Düşük güven uyarı metni: "Düşük güven oranı — lütfen bu alanı dikkatle kontrol edin"
- [ ] Sağlayıcı etiketi onay ekranında görünür
- [ ] rawText API yanıtında yer almaz
- [ ] provider alanı API yanıtında döner
- [ ] Tüm alanlar düzenlenebilir
- [ ] "Onayla ve Kaydet" butonu ile kayıt başarılı
- [ ] Hatalı alanlar düzeltilip kaydedilebilir

## 4. Vehicle Duplicate Protection

- [ ] Aynı plaka ile yeni araç oluşturma → mevcut araç güncellenir (409 değil)
- [ ] OCR confirm ile aynı plaka → mevcut araç güncellenir, uyarı gösterilir
- [ ] OCR confirm ile aynı VIN (farklı plaka) → uyarı gösterilir
- [ ] OCR confirm ile aynı plaka + aynı VIN → mevcut araç güncellenir
- [ ] OCR confirm ile farklı plaka + aynı VIN → uyarı gösterilir
- [ ] Vehicle create action ile mevcut plaka → Türkçe hata mesajı
- [ ] `Vehicle(workshopId, plate)` unique constraint DB seviyesinde çalışır

## 5. OCR Data Safety

- [ ] `POST /api/smart-capture/ocr` yanıtında `rawText` yok
- [ ] `POST /api/smart-capture/ocr` yanıtında `provider` alanı var
- [ ] `OcrLog` kaydı workshopId ile oluşturulur
- [ ] `ocr_capture` audit log kaydı oluşur
- [ ] `ocr_confirmed` audit log kaydı oluşur
- [ ] Başka workshop'un OcrLog kaydına erişilemez

## 6. Tesseract Lifecycle

- [ ] `OCR_PROVIDER=tesseract` ile birden fazla OCR isteği sıralı işlenir
- [ ] Tesseract worker başarısız olursa bir sonraki istek tekrar dener
- [ ] `terminateTesseractWorker()` export edilmiş

## 7. Integration Checks

- [ ] `/app/smart-capture/registration` → tam akış çalışır (upload → OCR → confirm → save)
- [ ] `/app/orders/new` → "Ruhsattan Doldur" linki çalışır
- [ ] `/app/vehicles/new` → "Ruhsattan Oku" linki çalışır
- [ ] `/app/intakes/new?customerId=...&vehicleId=...&source=registration` → kabul sihirbazı ön doldurma
- [ ] Sidebar → "Ruhsat Okuma" menüsü çalışır
- [ ] Confirm sonrası → müşteri ve araç kartları gösterilir
- [ ] Confirm sonrası → "Araç Kabulüne Devam Et" linki çalışır

## 8. Regression Checks

- [ ] Mevcut müşteri/araç CRUD çalışır
- [ ] İş emri oluşturma/duzenleme çalışır
- [ ] Kabul oluşturma çalışır
- [ ] Dashboard yükleir
- [ ] Mevcut rotalar etkilenmemiş
- [ ] Docker dosyası eklenmemiş
- [ ] Gerçek SMS/WhatsApp/ödeme entegrasyonu yok

## 9. Lint / Type Fixes

- [ ] `bun run lint` — 0 errors, 0 warnings (previous `_error` unused vars and `<img>` warnings fixed)
- [ ] `bun run typecheck` — passes
- [ ] `bun run build` — passes
- [ ] Cashbox routes: `catch` blocks use bare `catch` (no unused `_error` var)
- [ ] Vehicle detail: `<img>` replaced with `<Image>` from next/image (with `unoptimized`)

## Validation Commands

```bash
 bun run lint
 bun run typecheck
 bun run build
 bunx prisma validate
 bunx prisma generate
```