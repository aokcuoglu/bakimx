# BakimX v0.1.3 — Manual QA Checklist

## Storage Provider — Mock (Default)

- [ ] `STORAGE_PROVIDER` not set or set to `mock` — app starts without errors
- [ ] Photo upload via checklist works with mock provider
- [ ] Uploaded photo thumbnail appears in intake media gallery
- [ ] Mock provider does not require any storage env vars
- [ ] Build works without storage env vars

## Storage Provider — Supabase (if configured)

- [ ] `STORAGE_PROVIDER=supabase` with valid env vars — app starts
- [ ] Photo upload persists to Supabase Storage bucket
- [ ] Photo thumbnail loads via signed URL in gallery
- [ ] Public share page shows photo thumbnails via token route
- [ ] Missing `SUPABASE_URL` or `SUPABASE_SERVICE_ROLE_KEY` shows clear error
- [ ] Service role key not exposed in browser network tab
- [ ] Storage path includes workshopId and intakeFormId

## Storage Provider — S3

- [ ] `STORAGE_PROVIDER=s3` throws "henüz uygulanmadı" error
- [ ] App does not crash — error is caught and reported
- [ ] Mock provider still works when switching back

## File Validation

- [ ] JPEG file uploads successfully
- [ ] PNG file uploads successfully
- [ ] WebP file uploads successfully
- [ ] HEIC file rejected with Turkish error message
- [ ] Non-image file (e.g., PDF) rejected with Turkish error message
- [ ] File larger than 8 MB rejected with Turkish error message
- [ ] File size shown in error message (e.g., "9.2 MB")

## Photo Upload — Intake Detail

- [ ] Photo checklist shows required photos with red camera icon
- [ ] Photo checklist shows optional photos with muted camera icon
- [ ] "Fotoğraf çek / yükle" opens camera/file picker on mobile
- [ ] File input accepts only JPEG, PNG, WebP
- [ ] After selecting file, preview shows in card
- [ ] "Tekrar çek / değiştir" allows re-selection
- [ ] "Kaydet" uploads file and shows loading state
- [ ] After upload, checklist item turns green with "✓ Tamam"
- [ ] "Değiştir" button allows replacing photo
- [ ] "Kaldır" button removes photo from checklist

## Photo Upload — Add Photo Card

- [ ] Photo type dropdown shows all types with required/optional labels
- [ ] Camera/file picker works
- [ ] Preview shows selected photo
- [ ] File format info text shown: "İzin verilen formatlar: JPEG, PNG, WebP. Maksimum 8 MB."
- [ ] "Fotoğraf Yükle ve Kaydet" button shows when file selected
- [ ] "Fotoğraf Kaydı Ekle" button shown when no file selected
- [ ] Loading spinner shown during upload
- [ ] Upload error shows Turkish error message

## Intake Media Gallery

- [ ] Gallery grid shows uploaded photos (2 columns)
- [ ] Each card shows photo thumbnail
- [ ] Each card shows type label (e.g., "Ön", "Arka")
- [ ] File name shown if available
- [ ] File size shown in readable format (KB/MB)
- [ ] MIME type shown (e.g., "JPEG", "PNG")
- [ ] "Tekrar çek / değiştir" button on each card
- [ ] Delete button (Trash2 icon) on each card
- [ ] Loading overlay shown during replace operation
- [ ] "Dosya yok" placeholder when fileUrl is null
- [ ] Failed photo shows "Yüklenemedi" placeholder

## Photo Replace

- [ ] Selecting new file via "Tekrar çek / değiştir" triggers replace
- [ ] Old storage file deleted (if storageKey exists)
- [ ] New file uploaded and metadata updated
- [ ] Gallery refreshes with new photo
- [ ] Audit log entry: `photo_replaced`

## Photo Delete

- [ ] Delete button removes photo from gallery
- [ ] Storage file deleted (if storageKey exists)
- [ ] Database record deleted
- [ ] Checklist item reverts to "not taken" state
- [ ] Audit log entry: `photo_deleted`
- [ ] Mock provider delete is no-op (no error)

## Public Photo Rendering (/s/[token])

- [ ] Photos with file render as thumbnails in 3-column grid
- [ ] Photo thumbnails load via token-based route
- [ ] Loading spinner shown while photo loads
- [ ] Failed/missing photo shows placeholder icon with type label
- [ ] Photo checklist text shows "(Fotoğraf mevcut)" for uploaded photos
- [ ] Photo checklist text shows "(Kaydedildi)" for metadata-only entries
- [ ] No raw storage keys visible in page source
- [ ] No internal IDs visible (only photo ID used for thumbnail URL)
- [ ] Invalid token shows branded not-found page
- [ ] Expired token shows branded not-found page

## Cross-Workshop Photo Access

- [ ] User cannot access another workshop's photo via /api/photos?id=
- [ ] User cannot upload to another workshop's intake
- [ ] Public token photo route only serves photos belonging to share link's intake
- [ ] Token for workshop A's intake does not expose workshop B's photos

## Audit Logs

- [ ] `photo_uploaded` audit log created on upload
- [ ] `photo_replaced` audit log created on replace
- [ ] `photo_deleted` audit log created on delete
- [ ] `photo_upload_error` audit log on upload failure
- [ ] `photo_replace_error` audit log on replace failure
- [ ] Metadata includes type, storageProvider, sizeBytes as appropriate

## Mobile Responsiveness

- [ ] Photo checklist renders correctly on mobile
- [ ] Camera capture works on mobile with `capture="environment"`
- [ ] Gallery grid adapts to mobile (2 columns)
- [ ] Upload button large enough for touch (48px+)
- [ ] Replace/delete buttons touch-friendly
- [ ] Photo thumbnails load on mobile browsers
- [ ] File picker works on iOS Safari
- [ ] File picker works on Android Chrome

## Error Handling

- [ ] Upload with invalid file type shows Turkish error in UI
- [ ] Upload with oversized file shows Turkish error in UI
- [ ] Storage provider error is caught and shown in Turkish
- [ ] Photo load failure shows friendly placeholder
- [ ] No raw stack traces in any error response
- [ ] No English error messages in user-facing UI

## No Docker

- [ ] No Dockerfile exists
- [ ] No docker-compose.yml exists
- [ ] No Docker scripts exist

## No Real SMS/OCR/Payment Claims

- [ ] No real SMS integration code
- [ ] No real OCR integration code
- [ ] No payment/billing module
- [ ] Demo SMS notice still shown in approval section
- [ ] No WhatsApp Business API claims

## Build Verification

- [ ] `bun run lint` passes with 0 errors
- [ ] `bun run typecheck` passes
- [ ] `bun run build` succeeds
- [ ] Build works without DATABASE_URL
- [ ] Build works without storage env vars
- [ ] `bunx prisma validate` passes
- [ ] `bunx prisma generate` succeeds