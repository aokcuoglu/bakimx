# BakimX v0.5.1 — Manual QA Checklist

## Calendar Provider

- [ ] App starts with `CALENDAR_PROVIDER=mock` (default) without errors
- [ ] App starts with `CALENDAR_PROVIDER=google` without credentials — falls back to mock, no crash
- [ ] App starts with `CALENDAR_PROVIDER=google` with valid credentials — uses Google Calendar provider
- [ ] Calendar settings page shows correct active provider

## Calendar View (/app/calendar)

- [ ] Page loads showing calendar with day/week/month view tabs
- [ ] Default view is "Haftalık" (week)
- [ ] Appointments appear as blue events on the calendar
- [ ] Deliveries appear as green/emerald events on the calendar
- [ ] Maintenance reminders appear as amber events on the calendar
- [ ] Day view shows detailed event cards with time, title, plate, status
- [ ] Week view shows 7-day grid with event summaries
- [ ] Month view shows compact event indicators with "+N daha" overflow
- [ ] Navigation: Previous/Next arrows change date range correctly
- [ ] "Bugün" button returns to current date
- [ ] Clicking an appointment event navigates to /app/appointments/[id]
- [ ] Clicking a delivery event navigates to /app/orders/[id]
- [ ] Clicking a maintenance reminder event navigates to /app/reminders/[id]
- [ ] Empty state shown when no events in current period
- [ ] Legend shows event type color coding (Randevu, Teslimat, Bakım)
- [ ] Breadcrumb navigation works (Ana Panel > Takvim)

## "Hatırlatmaları Kontrol Et" Button

- [ ] Button is visible on calendar page
- [ ] Clicking button shows loading spinner
- [ ] After completion, results panel shows with 3 job types:
  - [ ] Randevu Hatırlatmaları
  - [ ] Bakım Hatırlatmaları
  - [ ] Teslimat Hatırlatmaları
- [ ] Each result shows: processed, sent, failed counts
- [ ] Success results show green checkmark
- [ ] Failed results show red X icon
- [ ] "Kapat" button dismisses results panel
- [ ] Page revalidates after check (events may update)

## Calendar Settings (/app/settings/calendar)

- [ ] Page loads showing current provider
- [ ] Mock provider shows "AKTİF" badge
- [ ] Google Calendar provider shows credential status (✓/○ for each env var)
- [ ] When Google selected without credentials, shows "KİMLİK BİLGİLERİ EKSİK" badge
- [ ] Configuration instructions shown with .env example
- [ ] Sync behavior documentation visible
- [ ] Breadcrumb navigation works (Ana Panel > Ayarlar > Takvim Ayarları)

## Calendar Sync Integration

- [ ] Creating an appointment triggers syncAppointmentToCalendar (check console in mock mode)
- [ ] Setting estimated delivery date on an order triggers syncDeliveryToCalendar
- [ ] Creating a maintenance reminder with due date triggers syncMaintenanceReminderToCalendar
- [ ] Sync failures logged to CalendarSyncLog table (check with prisma studio)
- [ ] Successful syncs logged with externalEventId

## Reminder Automation

- [ ] Appointment reminder job processes appointments within 24h
- [ ] Maintenance reminder job processes upcoming/due/overdue reminders
- [ ] Delivery reminder job processes ready_for_delivery orders with unpaid balance
- [ ] Each job execution logged to ReminderExecutionLog table
- [ ] Communication triggers respect customer SMS/WhatsApp/Email consent (inherited from v0.5.0)
- [ ] Deduplication: lastRemindedAt prevents duplicate sends

## Logs

- [ ] CalendarSyncLog entries created on sync operations
- [ ] CalendarSyncLog contains: provider, direction, status, entityType, entityId, externalEventId
- [ ] ReminderExecutionLog entries created on job runs
- [ ] ReminderExecutionLog contains: jobType, status, processedCount, sentCount, failedCount
- [ ] Communication logs still integrated (inherited from v0.5.0)

## Security

- [ ] Calendar sync logs are scoped to workshop (no cross-tenant data leakage)
- [ ] No Google Calendar credentials exposed in UI or API responses
- [ ] Customer communication preferences respected before sending reminders
- [ ] API routes require authentication
- [ ] Calendar settings page shows provider name only, not credentials

## Navigation

- [ ] Sidebar shows "Takvim" under "Servis" group
- [ ] Sidebar shows "Takvim Ayarları" under "Ayarlar" group
- [ ] Both links navigate correctly
- [ ] Active state highlights correctly on sidebar
- [ ] Mobile bottom nav still works correctly

## Database

- [ ] `bunx prisma db push` creates CalendarSyncLog table
- [ ] `bunx prisma db push` creates ReminderExecutionLog table
- [ ] All indexes created correctly
- [ ] `bunx prisma validate` passes

## Regression

- [ ] All existing features work as before (appointments, intakes, orders, quotes, reminders)
- [ ] Communication providers still work (SMS, WhatsApp, Email)
- [ ] Existing cron endpoint /api/cron/reminders still works
- [ ] App build succeeds (`bun run build`)
- [ ] Lint passes (`bun run lint`)
- [ ] Typecheck passes (`bun run typecheck`)