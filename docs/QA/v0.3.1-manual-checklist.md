# v0.3.1 Manual QA Checklist

## OCR & Smart Capture Foundation

### Setup

- [ ] `bun install` runs without errors
- [ ] `bunx prisma validate` succeeds
- [ ] `bunx prisma generate` succeeds
- [ ] `bun run build` succeeds
- [ ] `bun run lint` passes (0 errors, 3 pre-existing warnings)
- [ ] `bun run typecheck` passes

### OCR Provider Architecture

- [ ] `src/lib/ocr/types.ts` exists and exports `OcrProvider`, `RegistrationOcrResult`, `OcrFieldConfidence`
- [ ] `src/lib/ocr/mock-ocr-provider.ts` exists and exports `MockOcrProvider` / `getMockOcrProvider`
- [ ] `src/lib/ocr/openai-ocr-provider.ts` exists and exports `OpenAiOcrProvider`
- [ ] `src/lib/ocr/deepseek-ocr-provider.ts` exists and exports `DeepSeekOcrProvider`
- [ ] `src/lib/ocr/tesseract-text-extractor.ts` exists
- [ ] `src/lib/ocr/provider.ts` exists and exports `getOcrProvider()` / `resetOcrProvider()`
- [ ] `src/lib/ocr/registration-result.ts` exists with Zod schema
- [ ] `src/lib/ocr/prepare-registration-image.ts` exists (client-side validation)
- [ ] `src/lib/ocr/normalize-registration-image.ts` exists (server-side HEIC conversion)
- [ ] `src/lib/ocr/index.ts` barrel file exports all modules
- [ ] `OCR_PROVIDER` not set defaults to "deepseek" (requires DEEPSEEK_API_KEY)
- [ ] `OCR_PROVIDER=mock` explicitly selects mock provider
- [ ] Missing API keys produce clear Turkish error messages

### Smart Capture Registration Page

- [ ] `/app/smart-capture/registration` page loads
- [ ] Page shows camera/file upload area
- [ ] "Dosyadan Yukle" button works
- [ ] Mobile `capture="environment"` opens camera
- [ ] Invalid file type shows error message
- [ ] File > 8 MB shows error message
- [ ] Drag & drop works

### OCR Processing

- [ ] After image upload, "Ruhsat okunuyor..." animation appears
- [ ] Mock OCR returns demo data (with OCR_PROVIDER=mock)
- [ ] DeepSeek OCR processes image and returns fields (with DEEPSEEK_API_KEY)
- [ ] OpenAI OCR processes image and returns fields (with OPENAI_API_KEY)
- [ ] Error during OCR shows Turkish error message

### Confirmation Screen

- [ ] OCR result shown in editable form
- [ ] Warning banner visible: "Ruhsat okuma sonucu otomatik oneridir..."
- [ ] Uploaded registration photo shown as preview (for non-HEIC)
- [ ] All fields editable: plate, vin, ownerName, ownerSurname, phone, brand, model, vehicleType, modelYear, engineNo, registrationDate
- [ ] Plate field auto-uppercased
- [ ] "Yeniden Oku" button returns to upload step

### Record Creation (Confirm Route)

- [ ] "Onayla ve Kaydet" creates customer + vehicle
- [ ] New customer created with registration owner info
- [ ] New vehicle created with registration data
- [ ] Existing vehicle (same plate) is updated, not duplicated
- [ ] Existing vehicle (same VIN) is updated, not duplicated
- [ ] Multiple matches (plate matches one vehicle, VIN matches another) returns 409 error
- [ ] Customer matched by phone number
- [ ] Vehicle customer reassigned when different customer found by phone
- [ ] `vinConfirmed` flag set when VIN is 17 characters
- [ ] Success screen shows customer/vehicle cards with links
- [ ] "Arac Kabulune Devam Et" links to intake with source=registration

### Work Order Integration

- [ ] `/app/orders/new` shows "Ruhsattan Doldur" card
- [ ] Card links to `/app/smart-capture/registration`
- [ ] `/app/vehicles/new` shows "Ruhsattan Oku" link
- [ ] Link goes to `/app/smart-capture/registration`
- [ ] Sidebar shows "Ruhsat Okuma" entry
- [ ] Intake wizard skips steps when source=registration

### OCR Audit Log

- [ ] Prisma `OcrLog` model created in schema
- [ ] `bunx prisma validate` validates OcrLog model
- [ ] OCR operation creates `ocrLog` record with provider, rawText, extractedJson
- [ ] Confirm operation updates ocrLog with confirmedJson, confirmedAt, customerId, vehicleId
- [ ] OcrLog is workshop-scoped (workshopId filter)

### Security / KVKK

- [ ] OCR data scoped to workshop (workshopId required)
- [ ] Raw OCR text not returned in API responses
- [ ] No OCR data in public output
- [ ] User confirmation required before record creation
- [ ] Turkish information notices present

### FAQ Update

- [ ] Landing page FAQ "Ruhsattan sase otomatik okunacak mi?" now says OCR is live (not planned)

### Regression

- [ ] `/app` dashboard loads
- [ ] `/app/orders` list works
- [ ] `/app/customers` list works
- [ ] `/app/vehicles` list works
- [ ] `/app/intakes` list works
- [ ] `/app/quotes` list works
- [ ] `/app/appointments` list works
- [ ] `/app/reminders` list works
- [ ] `/app/parts` stock list works
- [ ] `/app/suppliers` supplier list works
- [ ] `/app/cashbox` cashbox works
- [ ] No console/hydration errors
- [ ] No Docker files present

### Non-Functional

- [ ] No .env files committed to git
- [ ] No real SMS/WhatsApp/payment/invoice integration (all mocks)
- [ ] No hardcoded secrets in source code