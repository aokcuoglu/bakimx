# BakımX v0.5.9 — Migration Baseline & Para Tutarlılığı

**Release Date:** June 2026
**Type:** Veritabanı + tutarlılık
**Migration:** ⚠️ Evet — migration baseline yeniden kuruldu

## Overview
Migration geçmişi temiz bir başlangıç (`0_init`) üzerine yeniden tabanlandı ve para/sayısal alanlarda tutarlılık geçişi yapıldı. Bu sürüm, prod'a otomatik `prisma migrate deploy` akışının güvenle çalışması için zemini hazırlar.

## Değişiklikler
- Migration baseline: `0_init` + `add_collection_payment`, `add_vehicle_plate_unique`, `add_photo_phase_and_missing_tables`, `add_technician_workspace`.
- Para/sayısal gösterim tutarlılığı (money-consistency) geçişi.
- 24 dosya (+3447 / −548), `schema.prisma` güncellendi.

## ⚠️ Migration notu
Baseline yeniden kurulduğu için, mevcut bir veritabanına uygulanırken dikkatli olunmalı. Staging'de gerçek-benzeri veriyle önce doğrulayın.

## Manuel QA
- Temiz DB'de tüm migration'lar sırayla uygulanıyor.
- Plaka tekilliği (`vehicle_plate_unique`) çalışıyor; tahsilat (collection payment) kayıtları tutarlı.

*Not: Bu sürüm notu git geçmişinden geriye dönük derlenmiştir.*
