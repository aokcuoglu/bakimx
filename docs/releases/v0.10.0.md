# BakımX — v0.10.0

**Durum:** Minor sürüm — tag `v0.10.0` · prod `app.bakimx.com` (AWS).
**Kapsam:** 22 commit / 9 PR — **340 dosya, +2743 / −6121** (PR #105–#111, #114, #115).
59 dosya silindi, 14 dosya eklendi; net **−3378 satır**.
**Migration:** **Yok.** Şema değişikliği bulunmuyor; `prisma migrate deploy` gate'i no-op geçer.
**Bağımlılık:** **Bir paket düştü** — `@react-pdf/renderer` (ölü hat, PR #109). Yeni paket yok.
**Env:** Değişiklik yok.
**Deploy:** `main`'e merge **otomatik** prod deploy'u başlatır (PR #104'ten beri, ~11-12 dk).
Tag push'u yalnızca GitHub Release oluşturur (`release.yml`), deploy etmez.

## Overview

Dört başlık: **sessizce bozuk olan bir deploy adımının onarımı**, **parça detayı özelliği**,
**Contabo döneminin kapanışı** ve **büyük bir ölü kod / yapı temizliği**. Satır sayısının net
eksi çıkması bu sürümün karakterini özetliyor: eklenenden çok kaldırılan var.

Sürümün asıl konusu ilk madde. Her deploy'da koşan "Vehicle-catalog seed" adımı, eklendiği
**2026-07-24'ten beri hem prod hem dev'de her seferinde başarısız oluyordu** ve bunu kimse
görmüyordu: adım bilinçli olarak non-blocking tasarlandığı için deploy'u durdurmuyor, yalnızca
bir uyarı satırı bırakıyordu. Araç kataloğu (marka/model/tip) prod'a elle yüklendiği için
kullanıcı tarafında bir belirti de yoktu. Bozuk olan şey **kendi kendini iyileştirme**ydi:
sıfırdan kurulan ya da restore edilen bir veritabanı kataloğunu asla dolduramazdı.

Kök nedeni ararken üst üste üç ayrı kusur çıktı ve her biri bir öncekini çözünce görünür oldu.

## Düzeltme — Katalog seed pipeline'ı (PR #105, #107, #110)

### 1. TLS: `self-signed certificate in certificate chain` (PR #105)

Seed script'i kendi `pg` havuzunu **çıplak** kuruyor (`new Pool({ connectionString })`) ve
`src/lib/db.ts`'in RDS uyarlamasını tekrarlamıyordu. RDS `rds.force_ssl=1` + `DATABASE_URL`'de
`sslmode=require`; güncel `pg` sürücüsü `require`'ı artık `verify-full` gibi yorumluyor ve
Amazon RDS CA'sı Node'un güven deposunda olmadığı için ilk `createMany` `P1011 /
TlsConnectionError` ile ölüyordu. `DB_SSL_NO_VERIFY=true` task-def'te zaten vardı — script onu
okumuyordu.

- **`src/lib/pg-connection.ts` (yeni)** — `buildPoolConfig(url)`; bayrak açıkken URL'den
  `sslmode` sökülür ve `ssl.rejectUnauthorized=false` verilir. Bağımlılıksız (yalnız tip
  importu) olduğu için Docker `/migrate` ağacına Prisma client çekmeden kopyalanabiliyor
- `src/lib/db.ts`, `prisma/seed.ts` ve seed script'i artık **aynı yolu** kullanıyor;
  uygulamanın bağlanma davranışı değişmedi
- Neden migration gate geçip seed'in patladığı da açıklığa kavuştu: Prisma CLI'nin migrate
  motoru `sslmode=require`'ı libpq semantiğiyle ele alıyor, `pg` sürücüsü almıyor

### 2. Bellek profili: fixture'lar tamamen belleğe alınıyordu (PR #107)

TLS düzelince task bu kez `exit=137` ile öldü. `--from-file` modu **dört fixture'ın tüm
satırlarını** belleğe alıyor, sonra insert sırasında `.map()` ile **ikinci bir tam kopya**
çıkarıyordu; `type_details` 37.937 satır ve `raw_payload` taşıyor.

- **`src/lib/catalog/ndjson-stream.ts` (yeni)** — `streamNdjsonBatches`; readline'ı `for await`
  ile tüketir, böylece gunzip akışı `onBatch` çalışırken duraklar (backpressure) ve tepe bellek
  dosya boyutundan **bağımsız** olarak tek partide kalır
- `--from-db` modu da dosyaları yazdıktan sonra aynı akışlı yoldan insert eder → tek kod yolu

### 3. Task boyutu: 512 MB'lık one-off task OOM ile öldürülüyordu (PR #110)

Akışlı okuma tek başına yetmedi — bağlayıcı kısıt dosya okuma değil **INSERT yolu**ymuş.
Prisma'nın istemci motoru `createMany` çağrıları arasında belleği geri vermiyor. Gerçek
fixture'lar üzerinde ölçüldü (yerel Postgres'e gerçekten yazarak):

| BATCH | tepe RSS | süre |
|---|---|---|
| 2000 | 1662 MB | 12.3 sn |
| 500 | 1179 MB | 12.8 sn |
| 100 | 675 MB | 13.5 sn |

Parti küçültmek 100'de bile 512 MB'ın altına inmiyor, yani tek başına çözüm değil. Ham SQL'e
geçmek değerlendirildi ve **elendi**: şema her alanı `@map` ile snake_case'e bağlıyor, ham SQL
bu eşlemeyi ikinci kez elle taşımak olurdu ve şema değişince sessizce bozulurdu.

- Parti boyutu **2000 → 500** (ölçülen ~500 MB tasarruf, hız kaybı yok)
- Seed `run-task`'ına task seviyesinde **`512 CPU / 2048 MB`** override'ı — yalnızca bu tek
  seferlik task için; **uygulama servisinin boyutu değişmedi**. ~3 dakikalık bir iş için maliyeti
  yok sayılır
- **Teşhis düzeltmesi:** workflow yalnızca jenerik `stoppedReason`'ı ("Essential container in
  task exited") yazıyordu; OOM'u adlandıran alan `containers[0].reason`. Artık ikisi de log'a
  **ve** uyarı mesajına giriyor — bir sonraki hata CloudWatch'a inmeden görülecek

### Doğrulama

Dev deploy'un kendi koşusunda, elle müdahale olmadan:

```
Catalog-seed exit code: 0   ·   cpu 512 / memory 2048
  vehicleBrand: +0 inserted (of 251, duplicates skipped)
  vehicleModel: +0 inserted (of 5767, duplicates skipped)
  vehicleType: +0 inserted (of 37937, duplicates skipped)
  vehicleTypeDetail: +0 inserted (of 37937, duplicates skipped)
Done.
```

`+0` = dev kataloğu zaten dolu → **idempotentlik** de doğrulanmış oldu. Ayrıca 10 yeni birim test
(`pg-connection` 5, `ndjson-stream` 5) bu iki regresyonu kilitliyor.

## Özellik — Parça Detayı modalı (PR #114)

İş emri parça aramasında satırlar yalnız ad/no/marka gösteriyordu; usta "bu parça aracıma uyar
mı, ölçüleri ne" sorusuna cevap alamıyordu. Oysa arama **zaten araç-scoped**
(`searchVehicleArticles` → `vehicle_type_id`) — bilgi vardı, görünmüyordu.

- Satırdaki **ⓘ** ile açılan Parça Detayı modalı: "Bu araca uygun" rozeti + eşleşen araç kaydı
  (motor + üretim aralığı), teknik özellikler (TR), OEM numaraları, EAN
- Görsel → mevcut `PhotoLightbox` ile tam ekran/zoom
- Muadil parçalar **tembel** yüklenir (yalnız bölüm açılınca çekilir)
- Sağlayıcı tarafı probe ile doğrulandı: `article-complete-details` **tek çağrıda** özellik +
  OEM + EAN + görsel + uyumlu araç veriyor → parça başına 1 faturalı çağrı, kalıcı cache. Bu
  sağlayıcıda bozuk olan `criteria-for-articles-and-vehicle` ucuna gerek kalmadı
- Yeni uç `GET /api/tecdoc/articles/[articleId]` mevcut `tecdocRouteGuard`'ın arkasında: auth +
  `partsCatalog` paket kapısı + atölye başına rate limit. Dönen veri global TecDoc kataloğu
  (kiracı verisi değil); ağır `compatibleCars` listesi (300+ kayıt) istemciye gönderilmiyor

## Altyapı — Contabo dönemi kapandı (PR #111)

BakımX 2026-07-21'de AWS ECS'e taşınmıştı; bu sürümle VPS dönemine ait her şey repodan
kaldırıldı ve dokümanlar tek bir `docs/` hiyerarşisinde toplandı (kök dizin 28 → 21 dosya).

- Kaldırıldı: `.github/workflows/deploy.yml` (donmuş SSH + compose deploy'u), `docker-compose.yml`
  (yalnız o workflow kullanıyordu), `scripts/{provision-vps,restore-db,db-migrate-prod}.sh`,
  `.env.production.example`, 188 satırlık `DEPLOY.md` runbook'u
- **Korundu** (AWS'e kritik): `Dockerfile`, `docker-entrypoint.sh` (ECR imajı bunlardan build
  ediliyor, migration kapısı entrypoint'in `exec "$@"` yolundan geçiyor), `docker-compose.local.yml`
  ve `scripts/local-reset.sh` (yerel geliştirme)
- **Sonuç:** prod'un tek deploy yolu artık `deploy-prod-aws.yml`. **Rollback bir DNS çevirmesi
  değil, ECS task-def geri alımıdır** (bkz. `docs/releasing.md`) — eski runbook'a göre hareket
  etmeyin
- Kök dizindeki `RELEASE.md`/`DEPLOY.md`/`DB.md` → `docs/releasing.md`, `docs/deployment/`,
  `docs/database.md`. `docs/releases/` ve `release.yml`'in gövde kaynağı değişmedi

## Yapı — Bileşenler domaine göre bölündü (PR #115)

`src/components/app/` 139 dosyalık düz bir yığındı; `src/lib` zaten 29 domain klasörüne
bölünmüşken UI tarafında bir özelliğin parçalarını bulmak alfabetik tarama gerektiriyordu.
Sınıflandırma tahminle değil **gerçek tüketicilerle** yapıldı: her dosya için import grafiği
çıkarıldı (hem `@/` hem göreli importlar çözülerek) ve dosya, kendisini tüketen rota/bileşenin
domainine yerleştirildi; 3+ domain kullanıyorsa `shared/`'a. Davranış değişikliği yok — yalnız
dosya yerleri ve import yolları.

## Temizlik — Ölü kod ve erişilemeyen yüzey (PR #106, #108, #109)

Silme ağırlıklı bu üç PR, sürümdeki −3585 satırın neredeyse tamamı. Davranış değişikliği
hedeflenmedi.

- **PR #106** — import-erişilebilirlik analizi (695 TS/TSX dosyası; Next.js dosya konvansiyonları
  ve testler entry kabul edilerek) sonrası hiçbir yerden erişilemeyen bileşen/modüller kaldırıldı.
  İçlerinde bir **isim tuzağı** da vardı: `components/app/kpi-cards.tsx` ölü, canlı olan
  `app/dashboard/kpi-cards.tsx`. Ayrıca 2026-07-01'de `/orders`'a birleştirilen `/intakes` sayfa
  rotalarından geriye kalan `revalidatePath("/intakes")` çağrıları temizlendi — var olmayan bir
  yolu revalidate etmek sessiz no-op'tu; her çağrı noktasının kendi tazelemesini yaptığı tek tek
  doğrulandı
- **PR #108** — hiçbir client `fetch()`'i ve server import'u olmayan **6 API rotası** ve yetim
  server action'lar kaldırıldı. Bunlar ölü koddan öte, auth'lu ama ulaşılamayan **saldırı
  yüzeyi**ydi; dördü mutasyon yapıyordu (ör. müşteri KVKK/iletişim izinlerini yazan
  `/api/communications/preferences` — aynı iş canlı yolda `customers/actions.ts` üzerinden
  yapılıyor). Canlı kardeş rotalar (`share-visibility`) korundu
- **PR #109** — 2026-07-05'te Claude Vision'a geçilirken emekli edilen **PaddleOCR sidecar'ı**
  (`ocr-service/`, Python OCR script'leri, `template.json`) ve hiçbir zaman devreye alınmamış
  **`@react-pdf/renderer` hattı** kaldırıldı. Canlı PDF yolu HTML→yazdır (`/p/[token]/pdf`,
  `/s/[token]/pdf`); ölü bileşen bu paketin tek tüketicisiydi, paket de bağımlılıklardan düştü.
  `public/logo-icon-light.png` de yalnız o bileşene aitti — **`logo-icon-dark.png` CANLI**
  (e-posta başlığı), karıştırılmamalı

## Yayın kapıları

Bu sürüm için sürüm dalında (`dev` = `7546c59` + bump) çalıştırıldı:

- `typecheck` — temiz
- `lint` — **0 hata**, 11 uyarı (v0.9.0'dan devralınan aynı `react-hooks/incompatible-library`
  deseni; bu sürümde yeni uyarı eklenmedi)
- `bun test` — **384 geçti / 0 hata**, 63 dosya (v0.9.1'e göre +19 test)
- `build` — başarılı

Ek olarak silme PR'ları (#106, #108, #109, #111) için referans denetimi yapıldı: silinen 59
dosyanın hiçbirine kalan import, `fetch()` çağrısı, asset referansı veya CI/script bağı yok;
tek eşleşmeler kaldırmayı **açıklayan yorum satırları**. `docs/releases/` ve `release.yml`'in
gövde kaynağı #111'in doküman taşımasından etkilenmedi.

## Notlar

- Prod kataloğu 2026-07-24'te elle seed'lendiği için marka/model seçicide **kullanıcıya yansıyan
  bir değişiklik yok**. Kazanım operasyonel: prod seed task'ı bu sürümden sonra **ilk kez
  gerçekten çalışacak** ve katalog kendi kendini iyileştirir hâle gelecek
- `deploy-prod-aws.yml` ve `deploy-dev-aws.yml` bu sürümde değişti (seed task boyutu + teşhis).
  Workflow dosyası `paths-ignore` kapsamında olmadığı için merge deploy'u tetikler
- **Rollback yolu değişti:** Contabo redeploy'u artık yok; geri alma ECS task-def revizyonuna
  dönmekle yapılır (bkz. `docs/releasing.md`)
- v0.9.1 sürüm satırı `CHANGELOG.md`'nin 0.9.x tablosuna eklenmemişti; bu sürümle birlikte
  geriye dönük olarak da eklendi. Sürüm `0.9.2` olarak hazırlanmışken, araya giren PR #114
  kullanıcıya görünen bir özellik eklediği için SemVer gereği **0.10.0**'a çevrildi

## Çıktıktan sonra

1. Prod deploy'unda **"Vehicle-catalog seed"** adımının uyarısız geçtiğini doğrulayın; log'da
   `Catalog-seed exit code: 0` ve dört tablo için `+N inserted` satırları görünmeli.
2. `app.bakimx.com`'da yeni araç ekleme akışında **Marka → Model** seçicisinin dolu geldiğini
   gözle doğrulayın (serbest-metne düşmemeli).
3. **Parça Detayı (yeni):** iş emri parça aramasında bir satırın **ⓘ** düğmesiyle modalı açın —
   teknik özellikler, OEM/EAN ve "bu araca uygun" rozeti gelmeli; görsel büyütme ve muadil
   parçalar bölümü çalışmalı. Paketi `partsCatalog` içermeyen bir atölyede 403 dönmeli.
4. **Bileşen refactor'ı (#115) sonrası** ana akışlarda gözle duman testi: iş emri detayı, araç
   kabul sihirbazı, müşteri/araç listeleri, ayarlar. Build ve testler yeşil, ama taşınan 139
   dosyanın kapsamı geniş.
5. Silinen yüzeyler için duman testi: ruhsat OCR taraması (Claude Vision yolu), müşteri
   oluştururken iletişim izinleri, iş emri paylaşım linki ve `/p/[token]/pdf` yazdırma çıktısı.
