# BakimX v0.3.1 — OCR & Smart Capture Foundation

**Date:** 2026-06-12

## Summary

BakimX v0.3.1 introduces the OCR & Smart Capture foundation for workshops. Users can upload or photograph a Turkish vehicle registration certificate (ruhsat), have OCR extract owner and vehicle fields, review/edit the extracted data, then confirm to auto-create or update customer and vehicle records. The flow integrates with the intake and work order creation pipeline.

## Features

### OCR Provider Architecture

- `OcrProvider` interface with `RegistrationOcrResult` type and per-field confidence scores
- **Mock provider** (`OCR_PROVIDER=mock`) — returns hardcoded Turkish registration data with 1.5s delay simulation
- **DeepSeek provider** (`OCR_PROVIDER=deepseek`, default) — Tesseract.js for text extraction + DeepSeek chat API for field parsing with retry logic
- **OpenAI provider** (`OCR_PROVIDER=openai`) — OpenAI Responses API with structured JSON schema output and vision input
- Async factory `getOcrProvider()` selects provider via `OCR_PROVIDER` env var
- Graceful fallback: when `OCR_PROVIDER=mock` or when no API key is set, provides clear Turkish error messages

### Smart Capture Registration Flow (`/app/smart-capture/registration`)

- **Mobile-first camera/file upload**
  - `capture="environment"` for direct camera capture on mobile
  - Drag & drop support
  - File upload button
  - MIME type validation (JPEG, PNG, WebP, HEIC/HEIF) and 8 MB size limit
  - Server-side HEIC-to-JPEG conversion via `heic-convert`
- **Image preview** — uploaded registration photo shown after OCR processing
- **Processing state** — animated scan indicator during OCR
- **Error state** — Turkish error messages for all failure modes

### Extracted Fields

OCR result structure with per-field confidence:
- `plate` — License plate
- `vin` — Chassis/VIN number
- `ownerName` / `ownerSurname` — Owner name/surname
- `brand` / `model` — Brand/model
- `vehicleType` — Vehicle type (e.g., Binek, Kamyonet)
- `modelYear` — Model year
- `engineNo` — Engine number
- `registrationDate` — Registration date
- `rawText` — Full OCR text (stored in DB, not returned to client)

### User Confirmation & Editing

- Extracted fields shown in editable form with warning banner
- All fields editable before confirmation
- Phone number required (min 10 digits) for customer matching
- "Onayla ve Kaydet" (Confirm & Save) creates/updates records
- "Yeniden Oku" (Re-read) returns to upload step

### Record Creation / Prefill (confirm route)

- Finds existing vehicle by plate or VIN (with duplicate detection)
- If matching vehicle found: updates empty fields, optionally reassigns customer
- If no matching vehicle: finds or creates customer by phone, creates new vehicle
- `vinConfirmed` flag set when VIN is 17 characters
- Creates `OcrLog` audit entry linking user, customer, vehicle
- Returns customer/vehicle IDs and direct intake URL with `source=registration` param

### Work Order Integration

- `/app/orders/new` — "Ruhsattan Doldur" card linking to smart capture
- `/app/vehicles/new` — "Ruhsattan Oku" link (now functional, not placeholder)
- `/app/intakes/new?customerId=...&vehicleId=...&source=registration` — skips customer/vehicle steps in intake wizard
- Sidebar navigation: "Ruhsat Okuma" entry

### OCR Audit Log (OcrLog model)

- `ocrProvider` — which provider was used
- `rawText` — raw OCR text (not returned in API responses)
- `extractedJson` / `confirmedJson` — before/after field values
- `confirmedAt` — when user confirmed
- `userId` / `workshopId` — who and which workshop
- `customerId` / `vehicleId` / `serviceOrderId` — linked records

### Security / KVKK

- OCR data scoped to workshop (`workshopId`)
- Raw OCR text not returned in API responses, stored only in DB
- User confirmation required before any record creation
- Turkish information notices throughout the flow

## Schema Changes

### New Model: `OcrLog`

```prisma
model OcrLog {
  id             String   @id @default(cuid())
  workshopId     String
  workshop       Workshop @relation(...)
  ocrProvider    String
  rawText        String?
  extractedJson  String?
  confirmedJson  String?
  confirmedAt    DateTime?
  userId         String?
  customerId     String?
  vehicleId      String?
  serviceOrderId String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
}
```

### Workshop Relation

- Added `Workshop.ocrLogs OcrLog[]`

## Routes

### New Pages
- `/app/smart-capture/registration` — Registration OCR page

### New API Routes
- `POST /api/smart-capture/ocr` — Run OCR on uploaded image, return result + ocrLogId + preview
- `POST /api/smart-capture/confirm` — Confirm fields, create/update customer & vehicle, return IDs + intake URL

### Modified Pages
- `/app/orders/new` — Added "Ruhsattan Doldur" card
- `/app/vehicles/new` — "Ruhsattan Oku" link now functional
- `/app/intakes/new` — Handles `source=registration` param to skip steps
- `app-shell.tsx` — Added sidebar "Ruhsat Okuma" entry

## Files Changed

### New Files
- `src/lib/ocr/types.ts` — OcrProvider interface, RegistrationOcrResult, OcrFieldConfidence
- `src/lib/ocr/mock-ocr-provider.ts` — Mock OCR provider
- `src/lib/ocr/openai-ocr-provider.ts` — OpenAI OCR provider (Responses API + vision)
- `src/lib/ocr/deepseek-ocr-provider.ts` — DeepSeek OCR provider (Tesseract + chat)
- `src/lib/ocr/tesseract-text-extractor.ts` — Tesseract.js text extraction singleton
- `src/lib/ocr/provider.ts` — Provider factory with env var selection
- `src/lib/ocr/registration-result.ts` — Zod schema + result converter
- `src/lib/ocr/prepare-registration-image.ts` — Client-side image validation
- `src/lib/ocr/normalize-registration-image.ts` — Server-side HEIC conversion + preview
- `src/lib/ocr/index.ts` — Barrel exports
- `src/app/api/smart-capture/ocr/route.ts` — OCR API route
- `src/app/api/smart-capture/confirm/route.ts` — Confirm API route
- `src/app/app/smart-capture/registration/page.tsx` — Smart capture page
- `src/components/app/smart-capture-registration.tsx` — Main client component
- `src/types/heic-convert.d.ts` — Type declarations for heic-convert

### Modified Files
- `prisma/schema.prisma` — Added OcrLog model, Workshop.ocrLogs relation
- `next.config.ts` — Added heic-convert and tesseract.js to serverExternalPackages
- `package.json` / `bun.lock` — Added heic-convert, tesseract.js dependencies
- `src/app/app/orders/new/page.tsx` — Added "Ruhsattan Doldur" card
- `src/app/app/intakes/new/page.tsx` — Handle source=registration param
- `src/components/app/app-shell.tsx` — Added sidebar entry
- `src/components/app/intake-wizard.tsx` — Prefill logic for registration source
- `src/components/app/vehicle-create-form.tsx` — "Ruhsattan Oku" now links to smart capture
- `src/components/sections/FAQSection.tsx` — Updated OCR FAQ answer (now live, not planned)
- `src/lib/format.ts` — Added normalizePhone utility
- `README.md` — Updated with smart capture documentation

## Validation

- **lint:** passed (0 errors, 3 pre-existing warnings — unused vars in cashbox routes, img element)
- **typecheck:** passed
- **build:** passed (Next.js 16.2.6, 55 routes, all compiled)
- **Prisma validate:** passed
- **Prisma generate:** passed (v7.8.0)
- **No Docker files:** confirmed
- **No .env files committed:** confirmed (.env.local is gitignored)
- **No real SMS/WhatsApp/payment/invoice integration:** confirmed (all mocks or client-side links)

## Known Limitations

1. No real OCR provider active by default — requires `DEEPSEEK_API_KEY` or `OPENAI_API_KEY` env vars for live OCR; falls back to mock otherwise
2. Mock provider returns fixed demo data (not real extraction)
3. Tesseract.js worker is a lazy singleton and never terminated (acceptable for server runtime, noted for future optimization)
4. No unique constraint on Vehicle(plate, workshopId) at DB level — duplicate detection is application-level only
5. Large images sent as base64 data URLs in JSON body (no multipart upload yet)
6. `vinOcrRaw` field on Vehicle model is defined but not populated in v0.3.1
7. DeepSeek default model is `deepseek-v4-flash`; OpenAI default model is `gpt-5.4-mini` — verify model availability
8. OpenAI provider uses Responses API (`/v1/responses`) — requires compatible model
9. HEIC conversion adds processing overhead; preview data URL stored in memory
10. Currency is TRY only

## Review Agent Findings (Addressed)

- **FAQ outdated:** Fixed — OCR FAQ now states feature is live
- **No Docker files:** Confirmed
- **No .env committed:** Confirmed
- **No real integrations:** Confirmed
- **3 lint warnings:** Pre-existing, not introduced in v0.3.1